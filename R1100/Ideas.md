A. 排序之后都移动到中间位置。

B. 每个i都取max(a[i], b[i])，然后枚举某个位置a[i]和b[i]都取，求max。

C1/C2. 最小化：尽量都变成负数，可以从后往前，碰到位置i为正数，就把[1,i]都变负。最大化：可以发现是没有办法全部变正数的，最好的情况就是，某个位置i的正数变负，然后它前面全部为正，它后面全部保持不变。所以枚举那个变为负数的正数的位置i。

***D. 二分最后的答案也就是max{min(a[1],b[1])} => max{a[1]}=X。trick 1）比X小的设为0，大于等于X的设为1，那么ab数组变成01数组。trick 2) 最后必然剩下(1,1)的pair，因此如果C1表示1的个数，C0表示0的个数，那么我们希望f=C1-C0>0。再看操作的意义，根据(0,0),(0,1),(1,1)的互相作用，可以发现我们希望尽量优先合并(0,0)和(0,1)这样可以去掉0，使得f变大。最后剩下的(0,0)和(0,1)可以和剩下的(1,1)去抵消并且消掉(0,1)从而保持f不变。如果还剩下(1,1)再看去掉这些1之后，是否依然可以保持f>0。***

A. 排序之后相当于是min每次+1，max每次-1，就看谁先到达mid。

B. 看只出现一次的字符，删掉之后-1，如果两边字符相同就再-1。

C1/C2. 发现操作相当于下标奇偶性相同的0和1交换位置，所以按照下标奇偶性分开，排序之后查看01个数相同。最少交换次数a和b之间1的下标delta的和。

D. 按照b从小到大排序，之后看连续相同的几个值，设有cnt个，从l到r，那么b[r+1]就应该等于前cnt个数的和并且这cnt个数也应该相等。贪心+一些check。

E. b中正数排序，负数排序。先从小到大放正数，当前sum减掉某个负数不会小于0了就减掉。

F. 发现不管操作多少次，结果都相当于某个数x不变，其他数全部XOR上它。n是偶数所以这时候所有数XOR起来就相当于是a中所有数XOR去掉x。所以枚举看去掉谁，a和b的XOR相同。找出这个数，XOR之后检查。

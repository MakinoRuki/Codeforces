A. 枚举d(y)是多少，再看y。

B. 把数组a相同数合并，把数组压缩。然后跟p中对应数去对比位置。

***C. 分情况讨论。1.博弈经典策略：follow对手的策略。如果p>=q，B只要跟A一样就可以永远p>=q。2.p-1/q-1一定比p/q小。如果p/q>=2/3，只要B跟A一样操作必然会drop到2/3；如果p/q<2/3，A就可以让它一直小于2/3。***

***D. 分块的思想。a[i]\*a[j]=j-i，那么如果a[i]和a[j]必有一遍小于等于sqrt(n)。枚举a[i]可以根下找到j，枚举a[j]可以根下找到i。去重就去掉ij都不超过sqrt的。*** 

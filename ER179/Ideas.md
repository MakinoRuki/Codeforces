A. 2个变得相等(假设都为x)之后，就继续把第三个变成2x+1，然后两个都等于2x+1，以此类推。

B. 先放最大的，然后只要第n-1个能放在h/w/l剩下的部分里，那么剩下的都能放进去。

C. 枚举。选一连串相等的位置，然后把左右都变成这个数。

D. 一个有点难写的模拟。把room按照楼层排序后，最低的和最高的match，让两拨人来回对调，次低的和次高的来回对调，以此类推。

E. 贪心。从前往后看每个位置是不是可以变得更小。b->a,b->c,b->c->a,c->a,c->b,c->b->a。用set把每个pair的下标存起来，只有b->c->a和c->b->a需要在set里upper_bound一下二次查找位置。

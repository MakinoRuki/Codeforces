A. 鸽巢原理。每一种都不能超过n(n-1)。

B. 用set和queue同时模拟。前k个放在set里，后面n-k用queue。

***C. 列一下式子，发现是类似sum=S\*c1+S(1-p1/100)c2+S(1-p1/100)(1-p2/100)c3+...，那么可以从后往前dp，dp[i]=dp[i+1]\*(1-p[i]/100)+C[i]。*** 

D1. 对于u->v，如果不存在一个其它的w，满足u->w->v，那么u->v之间就存在边。

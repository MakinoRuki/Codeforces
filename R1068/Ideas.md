B. 其实满经典的。要么是pre_sum-x要么是x-pre_sum，因此就dp一个到当前的max和min。

C. 从小到大贪心，筛法的一个变形。如果一个数x的所有不超过k的倍数都出现了就把它加进set里，然后被它mark过的数就不用再看了；否则看它是否已经被标记过，不行就直接无解。

D. 可以直接从低位到高位dp。dp[i,j,0/1]表示最低i位操作了j次，并且向i+1位进位为0/1时的最大得分。然后超过30位的部分dt一定是一位一位地进，因此就是直接+=dt-1即可。

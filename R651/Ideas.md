A. 答案就是n/2。因为>n/2的数字，gcd要等于它那么它的2倍至少应该<=n，但是取不到。

B. 只要让b中的和都是偶数就行。

C. 同时考虑2的power和奇素因子个数是否>1。

D. 就是二分！答案只取决于max较小的那一部分。然后枚举是奇数位置还是偶数位置的val必须小于等于枚举的mid，再O(n)贪心看能否选出长度>=k的子序列。

E. 首先01和10必须相等才有解。然后取一个计数器从左到右扫一遍，01就++，10就--，然后把中间得到的正值绝对值最大和负值绝对值最大加和就是答案。

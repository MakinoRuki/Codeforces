A. 只要倒序排列即可。

B. 其实不需要管坐在i之后p[i]被mark了怎么办，只要一直走下去，如果碰到mark了的i就把mark它的位置j(p[j]=i)删掉，然后坐上去j然后再往后看就好了，反正也不损失。

C1. 这种说任意[l,r]的gcd相同的，往往只需要看相邻位置不变即可。对于1和n只需要考虑gcd(b[1],b[2])和gcd(b[n-1],b[n])，但是中间位置i会被g1=gcd(b[i-1],b[i])和g2=gcd(b[i],b[i+1])影响，于是就把a[i]改成lcm(g1,g2)。

A. 最左边的几只必须向右，因此贪心调整，不是R就变成R直到够了k个；最右边几个必须向左，也贪心调整，不是L就变成L直到够了k个。

B. 最终相当于，一定可以选出数组里最大的几个元素。因为至少选x=ceil(n/2)个，然后如果大于c的数超过了x个，那么就选x个。

C. 首先n个数的最终mex必然是n，因此我们需要前n-1个数的mex是x=k^n。如果x<n，那么就把x拿出来其余数倒着放，最后把x放上；但如果x>=n，就把x的最高二进制power拆出来设为2^p，只要这个power不超过n，就可以让y=(2^p)^x，然后除了2^p和y都倒着放，最后再放y和2^p。

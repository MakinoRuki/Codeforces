A. 先把位于两个1之间的单个0变成1，之后把连续的1的段看min和max。

B. 直接枚举模拟就好了。机器人的策略是尽量均分，你的策略是把当前最大值变为0。再有就是n+1<=m时，机器人只要平均发展n+1个位置即可，否则要m个都平均发展。用一个set维护当前哪些机器人大于0，机器人首先填满min(n+1,m)，然后去填最少的；而你每次去掉最大的。

***C. 首先预处理出来每个位置放抽水机可以抽掉多少水res[i]，往左右枚举，维护之间的max。然后再枚举两个抽水机位置i和j，手玩一下可以发现要去重的部分就是，假设i和j之间的最大值是id，那么就相当于要去掉res[id]就刚好是重复的部分。***

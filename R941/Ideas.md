A. 直接模拟，用set维护，根据凑k需要的从小到大排列，逐渐满足。

B. 第1列和第m列是两个不同色的列，且列内全同色；或者第1行和第n行是这样的时候，是NO。

C. 如果最小一堆只有1那么只能取1，直到取到最小堆大于1了，那么当前play可以决定胜负。

***D. 拍脑门构造题。想到的部分：2^0->2^20都放一个，但是去掉k的最大的2的幂2^p。没想到的：这样其他含2^p的数字凑不成了，所以要加三个数进来，k-2^p,k+1,k+1+2^p。证明就是讨论：1<=i<=k-2^p时i<=2^p-1所以直接构造；k-2^p<i<=k-1时i-(k-2^p)<=2^p-1所以直接构造；i>=k+1时i-(k+1)如果包含2^p就减掉(k+1+2^p)可以直接构造。*** 
